1、redis 每次读写都会在键上修改它的使用次数，还会记录它最后一次使用时间，为的是方便过期键删除

2、redis 采用事件机制，时间事件机制处理，reactor模式，底层采用IO多路复用器，epoll

3、redis红锁：多个redis节点，这些节点相互独立，互不干扰，一般节点数量设置为单数（分布式系统节点单数设置），当加锁的时候，超过一半的节点加锁成功，就认为是成功，
当解锁的时候，向所有的节点发送del命令