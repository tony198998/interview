1、multi：开启事务
discard：放弃事务，之前的命令就不会执行
exec：执行事务，事务提交

自我感觉：redis开启一个事务，multi，exec命令，命令无错误，整个过程事务都可以提交；
        如果整个事务命令无错误，但是单个命令执行的时候，如 set  name aaa,incr name ,由于name不是数字类型，所以incr name报错，但是其他的命令却能够提交，
        所以它的事务，只针对命令无错误的事务，不兼顾运行时异常出错
        得出结论：redis是支持单条命令事务的，但是事务并不能保证原子性


乐观锁：采用watch命令监视key的value值，如果key的value值没有被改变，事务就执行，value值被改变的话，事务执行失败
悲观锁：一直监视这个key对应的value，没有执行当前步骤完成钱，其他任何线程不能执行，十分浪费性能

jedis：redis官方推荐的java连接工具